name: 'x3m'
version: '1.0.0'
config-version: 2

profile: 'x3m'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  x3m:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +materialized: table
